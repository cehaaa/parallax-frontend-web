const UserTransactionsHistory = () => {
	return (
		<div className='h-full overflow-y-auto bg-gray-800 p-5'>
			{/* {Array(50)
				.fill(0)
				.map((el, index) => {
					return <div key={index}>halo</div>;
				})} */}
			{/* <div className='flex h-full flex-col p-5'>
				<div className='text-2xl font-semibold'>Your Transactions History</div>

				<div className='mt-5 min-h-full grow '>

				</div>
			</div> */}
		</div>
	);
};

export default UserTransactionsHistory;
